version: '3.8'

services:


  backend:
      build: ./backend
      container_name: kanan_backend
      environment:
          - DATABASE_URL=mongodb+srv://kachhelajeet3_db_user:SRs8q3pnkCqKsueH@cluster0.qdrn2va.mongodb.net/agent_survey?retryWrites=true&w=majority&appName=Cluster0
          - SECRET_KEY=${SECRET_KEY}
          - ALGORITHM=${ALGORITHM:-HS256}
          - ACCESS_TOKEN_EXPIRE_MINUTES=${ACCESS_TOKEN_EXPIRE_MINUTES:-480}
          - GROQ_API_KEY=${GROQ_API_KEY}
      volumes:
          - ./backend/app/uploads:/app/app/uploads
      ports:
          - "8000:8000"

  frontend:
      build:
          context: ./frontend
          args:
              - VITE_API_URL=${VITE_API_URL:-http://localhost:8000/api}
      container_name: kanan_frontend
      ports:
          - "80:80"
      depends_on:
          - backend

